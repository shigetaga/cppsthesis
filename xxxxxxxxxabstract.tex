%
% ここのオプション（第1引数）は変更可能・追加自由（ただし tate と landscape は禁止）
\PassOptionsToClass{head_space=20mm,foot_space=20mm,fore-edge=16mm,gutter=16mm,fontsize=9pt}{jlreq}

% 強制オプションとクラスファイル読み込み（書き換え禁止）
\documentclass[paper=a4,article,oneside,twocolumn,notitlepage]{jlreq}

%% -----設定ここから----- %%
%% \ModifyHeading 以外は原則として触らないこと
  \makeatletter
  % tate や landscape が使われてたら即座に停止
  \if@landscape\errmessage{DON'T use "landscape" option!!}\fi
  \if@tate\errmessage{DON'T use "tate" option!!}\fi
  % 1ページ目の一番上に出力するもの
  \DeclareRobustCommand*\titleitems[6]{
    \gdef\@maketitle{%
      \fontsize{9pt}{12pt}\selectfont\sffamily\gtfamily
      \begin{center}%
        \ltjsetparameter{kanjiskip={0.05\zw plus 0.1\zw minus 0.05\zw}}%
        \vspace{9pt}%
        \vtop{\fontsize{18pt}{24pt}\selectfont{#1}}%
        \vspace{12pt}%
        \ifx#2\@empty\else\vtop{{#2}}\vspace{6pt}\fi%
        \vtop{{#3}\hspace{\zw}{#4}\hspace{\zw}{#5}}\vspace{6pt}%
        \vtop{指導教員：{#6}}%
        \vspace{15pt}\vtop{}%
      \end{center}%
    }
  }
  % 本文開始時に自動で出力
  \AtBeginDocument{\begingroup\let\@nodocument\relax\maketitle\endgroup}
  % ページ番号は入れない（必須）
  \pagestyle{empty}
  \let\pagestyle\@gobble
  \let\thispagestyle\@gobble
  % 節見出しの設定（目安）
  \ModifyHeading{section}{font={\jlreq@keepbaselineskip{\normalsize\sffamily\gtfamily\bfseries}}}
  \ModifyHeading{subsection}{font={\jlreq@keepbaselineskip{\normalsize\sffamily\gtfamily\bfseries}},indent=\zw,subtitle_font={\jlreq@keepbaselineskip{\normalsize}}}
  \ModifyHeading{subsubsection}{subtitle_font={\jlreq@keepbaselineskip{\normalsize}},indent=2\zw,before_lines=0}
  \makeatother
%% -----設定ここまで----- %%

% ここからプリアンブルです。\usepackage{ナントカ} したり \newcommand\本質{雪だるま} したりできます。

% 最終ページの2段の行数をなるべく揃えたいときは
% \usepackage[balance]{nidanfloat}
% が有用（1ページしかないときに使うと表示が壊れるので注意）

% 1ページ目の一番上に出力するものはここに記入（空行・\par はエラー）
\titleitems{}% 卒業研究題目
           {}% 副題（無い場合は{}のままでOK）
           {}% 主専攻
           {}% 学籍番号
           {}% 著者氏名
           {}% 指導教員氏名・職名

\begin{document}

% ここに本文を書きます。

\end{document}